# EnvBox

A UI for managing environment variables — organize, search, and export `.env` files.

## Features
- Group and search environment variables
- Export to `.env` format

## Run locally
```bash
npm install
npm run dev
```
